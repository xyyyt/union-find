# union-find
Container implementing a union-find / disjoint-set data structure, optimized for efficient merging and connectivity tracking in graph-related problems.

## Dependencies
To compile union-find project, you need the following packages :
- googletest

```sh
apt-get install googletest
```